#ifndef SOFTPC_CMOS_HOST_H
#define SOFTPC_CMOS_HOST_H

#include "insignia.h"

/* Standalone host port for the actually attached fixed-media topology. */
extern half_word softpc_host_cmos_fixed_disk_type(void);

#endif
