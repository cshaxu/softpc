# types

`types` is the root component. It provides scalar aliases, status values,
portable atomic helpers, and small C-runtime vocabulary for memory, text,
bounded formatting, allocation, and opaque byte-file primitives. Every other
component may depend on it; it has no component dependency or product I/O
policy.
