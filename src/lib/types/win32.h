#ifndef LIB_TYPES_WIN32_H
#define LIB_TYPES_WIN32_H

/* The sole shared-library inclusion point for the Win32 SDK vocabulary.
 * Component win32 sources may use its typed definitions but never include the
 * SDK directly. */
#include <windows.h>

#endif
