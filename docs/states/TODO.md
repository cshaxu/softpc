# Long-Term Review Ledger

No deferred work is recorded. Add an item only after an admitted task has
identified work that is real but intentionally not queued. Every entry uses
`- [ ] **Title (`TODO(High|Medium|Low)`).**` and names its owner/boundary and
admission condition.
