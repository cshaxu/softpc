# M9 Td S11: common UI ownership

Owner approved common/session deciding required actions and common/ui owning
monitor Console, raw Console, Window, and broker handoff. Commit `5707ee8`
updated and pushed the queued proposal, removing the extra common/console and
common/kvm split. Documentation validation passed.

This record restores the history index for the already-used identifier; it
does not reopen the task. The live candidate is
[common convergence](M9-T56-common-convergence-proposal.md).
