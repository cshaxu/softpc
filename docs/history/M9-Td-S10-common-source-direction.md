# M9 Td S10: common source direction

Owner rejected an NXVM-first common replacement because the accepted SoftPC
experience is the reference. Commit `018598b` changed the queued proposal to
extract common from SoftPC first, import debug/xasm32 later, and offer the
result for unchanged NXVM adoption. Documentation validation passed.

This record restores the history index for the already-used identifier; it
does not reopen the task. The component split proposed then was superseded by
[Td S11](M9-Td-S11-common-ui-ownership.md).
