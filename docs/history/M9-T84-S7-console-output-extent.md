# M9 T84 S7 — Console output-extent repair

S7 delivered `5d7b2619`, which restored the raw Console write extent to
`max(active rows, previously committed rows)`. A steady 25-row frame no longer
uses host viewport height, and a 50-to-25 transition still clears the confirmed
old tail. Focused broker tests, dual-width background regression and
documentation checks passed.

The owner then admitted S8 to combine this retained rule with NXVM's separate
capacity-only surface preparation. S8 is the acceptance vehicle for the joined
Console contract; S7 adds no independent product branch or unreviewed API.
