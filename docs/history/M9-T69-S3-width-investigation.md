# M9 T69 S3 bounded closure

Owner explicitly directs closing S3 and admitting S4 to audit the whole T69
change set. This is a bounded investigation closure, not a T69 completion or
a claim that every display symptom is repaired.

## Requirements and disposition

| Requirement | Disposition and receiver |
| --- | --- |
| Explain and remove initially windowed Prompt width oscillation | Real guest A/B isolates BIOS bookkeeping used as packed width; P1 uses the selected painter row contract. Owner accepts P2 for initial windowed and returned-windowed Prompt. |
| Audit S2 mode-settle mechanism | Continuous invalidation can indefinitely postpone selection. P2 removes generation rearming and restores original bounded countdown; focused regression covers 20 invalidating ticks. |
| Actual fullscreen/windowed roundtrip | P7 published-frame probe has no width above 640 and legible final text. Earlier corruption/typing reports are not comprehensively accepted; retain in T69 proposal and S4 audit ledger. |
| New fullscreen native Window height oscillation | Owner and probes cannot reproduce; actual outer Window, possibly after prior windowed Prompt. Owner asks to transfer to TODO; no speculative repair. |
| Code quality and mirror diff | Explicitly transferred to newly admitted S4, covering all T69 production/test changes rather than S3 alone. |

## Delivery and limitations

Production packages remain `19533be`; later P commits add tests and evidence.
Against S3 admission `3c49487`, tracked production changes are +12/-34 (net -22)
in three MVDM files; test changes are +28/-12 (net +16) in VGA smoke. The original
gfx_upd/gfx_updt generation additions have no remaining T69 net diff.

Expanded packed-mode tests cover all 256 BIOS record values without display
register writes. x86/x64 focused VGA tests pass. P2 full results remain x64
105/108 and x86 106/108: package stage-16 failures and one x64 BIOS-tick failure
were not proven unrelated. The isolated tick rerun passed, not an erasure of
the full-run result. No new full-suite claim accompanies documentation closure.

Real diagnostics use in-memory overlay; media/configuration remain unchanged.
Temporary targets, traces and probes are removed. Detailed root-cause and
probe evidence remains in the active T69 proposal, S3 P1 through P8.
