# M9 T61 S6 — Final mirror audit and delivery

## Request and convergence

Owner requested a complete original-diff explanation and safe minimization,
not source-line compression or feature removal. The admitted S1-S6 proposal
remains [the retained task plan](M9-T61-mvdm-diff-minimization-proposal.md).
The finite ledger is [the mirror audit](../etc/evidence/softpc/pristine-divergence-current.md).

The original 498-path universe is unchanged: 403 byte-identical, 95 divergent,
0 without an original peer; 650 original paths remain intentionally unselected.
Raw and auxiliary final totals agree at +23,141/-22,335, 5,132 hunks.
Against comparable S1 content totals this removes 737 changed lines and two
divergent files. Checkout newline noise is reported separately, not sold as
functional reduction. All 34 C-VID rule patterns remain proved and unchanged.

| Owner request / stage | Completed disposition |
| --- | --- |
| T60 handover | Owner manual acceptance and closure recorded before T61 admission |
| S1 complete inventory | Same-path hashes, all hunks, rule patterns, per-file reasons and two counting modes |
| S2 needless differences | Original whitespace restored; disabled DIB branch removed; necessary ABI declarations retained |
| S3 original conditionals | ios and non-CCPU declarations restored, selected preprocessing/code equivalence proved |
| S4 keyboard | One original table/algorithm, original additions 449 to 13; complete table and modifier/make/break tests |
| S5 sound | One original state machine, original additions 222 to 64; controlled clock/tone test before and after |
| S6 complete review | All remaining ledger dispositions reconciled; four final indentation-only lines restored |
| Preserve product and boundaries | No shared-corpus, App/VM/Compat, media/INI changes; no D6 removal, overlay, new owner or external build dependency |

Implementation P1 is `4f7171d`. S6 itself changes only four leading-indent
lines (+4/-4) in nt_cga/nt_ega, preserving the C syntax fix and painter bounds.
All other bytes/tokens in those changed lines are identical. Whole-tree
uniquely paired whitespace-line candidates are now zero; this does not assert
that no future redesign could reduce any necessary original difference.

Whole-task production delta is 14 mirror files +176/-715, net -539; tests
+111, build wiring +9, aggregate net -419. Read-only audit tooling +89 is
separate. Remaining reasons and original-source reduction by stage are in the
ledger. No duplicate implementation moved into Compat.

## Coordinator acceptance

Reviewed the actual pushed P1 source diff, original-format lines, ledger and
protected-path diff against 893db1e. Both builds pass; full suites are x64
98/98 (82.54s), x86 98/98 (72.11s). Actual-commit focused IRQ, VGA, BOP, x87,
keyboard, sound/state and restart checks pass 8/8 per width. Fixed artifact
hashes are in the ledger. Documentation and diff gates pass; no S6 temporary
research directory remains and no other task's build directory was removed.

One explicitly transferred issue remains: the pre-existing original keyboard
equal-length index defect in TODO, requiring separate behavior-repair admission.
It is not a missing T61 minimization. Existing Queue candidates are unchanged.
No real-desktop/RDP/audio manual acceptance is claimed from automated tests.

S6 closes as verified delivery. T61 remains open awaiting owner package testing;
there is no next active implementation S and no queued task is auto-admitted.
The implementation plan is archived out of the candidate-only proposal directory;
this is not T-level owner acceptance. The idle marker and archive links were
corrected after the documentation gate rejected the initial waiting-state wording.
