# Overlay 页索引优化

## 来源与状态

所有者要求：“overlay 页查询 O(n) 这个加入一个队列proposal，td提交”。
从 storage 性能 TODO 提升为候选后，所有者要求移至队首，并现已准入为 T76。
原始准入要求：“收口T75，准入下一个T任务：优化 lib/storage overlay查找表现”。
T75 已收口。所有者随后要求“不测量，直接上手修复”及“直接把S2内容做了”；
S1 测量取消，直接进入 S2。正确性和快照验证仍属于 S2/S3，全部完成后等验收。
采用已列出的直接数组方案，不引入备用策略；不承诺未经测量的具体加速倍数。
数组按总容量占用内存，可能比旧稀疏链表更早遇到 NO_MEMORY；保留明确失败，
不设置人为容量上限。此前测量门槛按本次明确指示撤销。
T55 S4 曾明确延期算法改动；本次只安排候选，不声称已发生产品性能故障。

本次原始要求：“这个方案挺好的。请你把这个优化方案写入proposal，并且提到队列首位。”

## 问题与设计约束

`src/lib/storage/medium.c` 当前每次查找 dirty page 都从链表头线性扫描。
单页查询 O(n)，大量不同 dirty page 的访问可能累计成平方级查找成本。
先用可重复的稀疏/密集、大量 dirty page 工作负载建立耗时和内存基线，
再选择最简单且有实际收益的页索引。不为理论复杂度引入通用容器框架。

保留现有公开 API、4 KiB 页覆盖行为、只读底层与内存 dirty 数据所有权。
不增加 commit 能力，不改变 direct/readonly 模式，不修改用户介质。

## 首选方案：按页号直接索引

磁盘容量在打开时已知。仅 OVERLAY 使用页指针数组，每个元素对应一个
4 KiB 页；元素数为容量除以 4096 后向上取整。空指针表示未修改，继续读
底层文件；首次写入分配页并读入原内容，成功后才安装指针。后续访问直接
按页号索引。无底层文件的 zero overlay 保留原有补零语义。

数组成为页的唯一所有者，删除页节点的 `index`、`next` 和链表查找；不保留
第二套链表或索引。销毁时释放非空页及数组。公开 API、独占 lease 和已有
跨页写入失败语义均不变，不增加事务、线程或通用容器框架。

页数计算不能使用可能溢出的“容量 + 4095”；数组字节数也须校验乘法边界。
零容量保持合法且不依赖零字节分配结果。分配/原页读取失败不得安装半成品
页或破坏已有内容；创建失败清理、尾页及销毁仍沿现有唯一生命周期处理。

### 内存与性能取舍

查询由 O(修改页数) 变为 O(1)，但数组内存与介质总容量有关，初始化/销毁
需要遍历数组。数据页仍按需分配，并非在内存中复制整个磁盘。

| 介质容量 | x64 指针数组 | x86 指针数组 |
| --- | --- | --- |
| 504 MiB | 1008 KiB | 504 KiB |
| 2 GiB | 4 MiB | 2 MiB |
| 1 TiB | 2 GiB | 1 GiB |

以上仅为指针数组，不含数据页与分配器开销。不能因为当前 SoftPC 使用小盘，
就静默缩小共享 Lib 的可用容量范围。实施前比较小盘、大量修改页和大型稀疏
介质的时间与内存成本；若直接数组代价不合适，先汇报证据，再评审两级按需
页表。两级页表是备选，不与直接数组同时维护成两个运行策略，也不默认引入
哈希表、平衡树或动态策略切换。

## 组件范围与快照兼容性

生产修改仅在 `src/lib/storage/medium.c`，如无新证据，不改公共头文件、
Common、Core、App 或 x86。测试主要在 `test/lib`，产品快照回归可以补充
必要测试，但不改其编码实现。

已核对 `src/core/compat/media_snapshot.c`：保存通过
`lib_storage_medium_read_at()` 按偏移扫描、比较原始和有效内容，编码介质
元数据及差异块的偏移、长度、数据；加载通过 `lib_storage_medium_write_at()`
重放。它不序列化 Storage 内部链表、指针或节点结构。

因此只换内部索引不会改变快照字段、差异块顺序或跨 x86/x64 格式，不需要
新版本号、转换器或旧格式兼容分支。同一基础介质和有效内容必须得到逐字节
相同的快照介质段。完整磁盘扫描及 SHA 计算仍存在；本优化只消除扫描时的
链表查找成本，不承诺消除 I/O 或哈希成本。

## 拟议实施拆分

1. S1：所有者取消测量，原计划不执行；现有旧版快照程序保留给 S3 作兼容证明。
2. S2：在 Storage 内替换唯一页索引，删除旧节点/遍历机制，补齐正常及失败
   测试。按所有者确定的直接数组方案实现，不混入另一套索引策略。
3. S3：完成旧快照加载、介质段编码一致性、双宽度往返及产品回归；汇报索引
   复杂度、明确内存代价和逐组件增删行数，交付双 EXE，等待所有者验收后收口 T。

每个实施 S 开始前报告方案、文件范围和预估增删；完成后报告实际增加、删除、
净变化及构建测试结果，提交推送完整交付。不得将文档登记视为实施已准入。

## T76 有限验收台账

冻结范围：Storage overlay 的页查找、页所有权与公开读写行为；所有调用方
仍走原 API，不涉及磁盘控制器、snapshot 编码、线程结构或其他组件重构。
每项须有实测或源码证明；若首选方案不能满足范围，先修订设计并说明代价。

| 项目 | 负责 S / 验证 |
| --- | --- |
| 查找成本与内存代价 | S2/S3：源码确认 O(1) 索引，按容量/位宽报告数组内存；所有者取消运行时性能测量，不承诺加速倍数。 |
| 唯一索引和所有权 | S2：移除链表字段和遍历；不同时维护备用策略或页所有权。 |
| 公开行为 | S2：零容量、同页、跨页、尾页、零基底和文件基底，readonly/direct 不分配 overlay 索引。 |
| 失败与边界 | S2：容量运算、分配和原页读取失败，未安装页清理及旧内容保留，保持原有跨页部分成功语义。 |
| 快照不变 | S3：从旧实现重建介质段基线；旧快照读取、编码字节一致及双位宽往返。 |
| 共享边界与交付 | 各 S：C11、manifest、边界检查、双宽度构建和适用后台测试；S3 完整回归和双 EXE，待所有者验收。 |

### S1 原始准入简报（已被所有者取消）

基线 eacf1a5e。生产代码估计 +0/-0；测量工具及测试增删须在读取现有测试
后作二次精确估计，不将一次性探针变成公共框架。先检查现有唯一页分配、
销毁和三个创建入口，复用已有测试及快照辅助设施。小盘、大量已修改页、
大型稀疏介质分别计量；避免真的分配数 GiB 数组来证明公式。
只用任务自有临时输出，保护 assets 和用户介质；所有桌面测试保持排除。
S1 的结论必须决定首选直接索引是否适合，而不是先实现再补解释。

## 实施验收

### S2 实际交付

medium.c +39/-30，净增9行（预估+35/-20）；原有 Storage 测试 +78/-2，
净增76行（预估约+100/-3）。合计 +117/-32，净增85行，仅两个 C 文件；
两项 manifest、文档和 EXE 不计入代码。页数组是唯一所有者，页只保存4096字节，
删除旧节点 index/next、find_page 和链表销毁。共同容量初始化供三种创建入口
复用；空介质不分配数组，Direct/Readonly 不分配数组。只有完整基底读取后的
页才安装，原有 range 校验及跨页部分写入语义不变。

扫描 medium 所有创建/read/write/fill/destroy 及 snapshot 公共消费者：
只有 overlay 的 read 与 page_for_write 两处索引访问；没有残留链表查询。
最大 lib_size 页数运算及分配字节数使用注入拒绝验证，不真的申请巨大内存。
测试覆盖五个逐次失败点、文件 index 失败关闭句柄、原页读取失败不安装、
重复写不分配、尾页、零容量、零基底及未修改页。定向双宽度通过。

双宽度严格 Release 构建通过；最终后台 x64 110/110（185.55s），
x86 110/110（170.30s），每宽度排除5项 desktop。S1 提前启动的旧测试运行
因实施中 manifest 改变失败，不作为验收证据；上述 S2 完整重跑全部通过。
SHA256：x86 D61305E116367F8CBD5B885351D81A7FA26FD74B5981AD66F3F7FFEF02206516；
x64 4DF851DB91A4559AE7C3E6CEB35753040355FE53CC4A8C2417C6E79EC9A5C8A0。

测试同页重复读写、跨页和尾页、稀疏覆盖、未修改页读取、分配失败及销毁。
同时覆盖零容量、索引容量计算边界、原页读取失败、zero overlay，以及
direct/readonly 不分配 overlay 索引且行为不变。
优化前后数据结果一致；报告额外内存，不以未测量的加速倍数作为交付事实。
对相同介质状态比较优化前后快照介质段字节，并验证旧快照加载、软硬盘
overlay 恢复及 x86/x64 往返；用隔离的小型测试介质，不污染 assets。
保留上述所有者取消测量的明确变更记录，不把取消项目标为测量通过。
实施后按修改范围构建双宽度 EXE、执行共享库和产品回归并提交推送。

### S3 实际交付与最终覆盖

生产 +0/-0；test/unit/media_snapshot_smoke.c +11/-0，净增11行，
比约12行预估少1行。使用原有两盘 overlay、跨页及尾页场景，无新框架或介质资产。
以 ffb8c36b 的原 medium.c 分别编译两位宽旧链表 codec 探针，输出均为17662字节，
SHA256 93d15a717c8ab7f3f78db31f6d666509a0845f2e2110c8ef07d0d0a15df511f4。
上述固定长度和摘要进入现有永久测试；新版双宽度全部匹配，证明这组介质内容
及元数据的编码没有随内部索引改变，不把整个运行机器快照误当确定性字节流。

另用修改前保留的两份 snapshot-transaction 测试程序（S2源码改动前生成）
以及当前双宽度程序组成 old32/old64/new32/new64：四个保存者分别输出快照，
每份由四个加载者加载，共16/16通过。原有加载断言验证软硬盘 overlay、
PAUSED 状态、resume 后真实 CPU 改画面；每次加载后基础镜像 SHA 未改变。
这也覆盖新保存/旧加载及两个跨位宽方向，不只覆盖新版自写自读。

最终双宽度 Release 重建通过；后台 x64 110/110（146.29s）、
x86 110/110（146.52s），每宽度排除5项 desktop。双 EXE 与 S2 哈希一致。
两份 EXE 较 T75 均增加512字节；没有额外运行时组件。未执行性能测量，
不声称具体加速倍数或全树无缺陷。任务自有探针、旧测试程序、临时镜像、
快照及日志在保留本证据后清理，不碰 assets 介质或用户 INI。

有限台账逐项已覆盖：查找源码 O(1)，唯一数组所有权，公开数据及失败行为
由 S2 测试验证，快照由本节证明，清单及边界由最终回归证明。
全 T 代码合计 +128/-32，净增96行：生产净增9行，测试净增87行；
文档、manifest 和 EXE 不计入代码。S1 按所有者要求取消测量；S2/S3完成
后停止，T76 保持开放，等待所有者验收再正式收口。
