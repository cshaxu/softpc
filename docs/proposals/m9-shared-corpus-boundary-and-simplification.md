# Lib / Common / x86 边界修复与精简

## T80 准入

所有者请求：“测试通过 可以收口t任务 准入下一个t”。T79 已通过手测并收口，
本队首提案正式分配 T80；基线 `b9413d65`。以下七项沿用为 S1--S7，当前
只准入 S1。原候选记录和审计证据保留，实际状态以 CURRENT 为准。
本次交接只修改文档，生产/测试代码 +0/-0，EXE 不变。S1 在动代码前先
复核取码、跳过和调用者路径，报告具体文件与增删估算，再执行实现与双宽度验证。

## 原始请求与准入边界

所有者先要求“审计一下，lib/common/x86六组件代码质量”，随后要求：
“将上述置于队列首位，写入proposal”。

本提案承接该次审计的全部七项结果，作为未编号队首候选。登记不分配 T 编号，
不准入实现，不改变 CURRENT 中的唯一活动任务。正式准入时重新冻结源码基线，
以下 S 编号仅表示建议的串行拆分。

审计读取的是 T78 实现 `0fb40f48` 所对应的共享源码；期间主线程继续更新治理
文档。六目录 `src/{lib,common,x86}`、`test/{lib,common,x86}` 共 204 个文件
完成清单核对，六份 manifest 和三个源码依赖检查通过。重点阅读了输入、帧、
同步请求、Storage 和 x86 编解码路径；这不等于逐行证明全部源码无缺陷。
此次未运行动态故障探针、虚拟机或完整回归。

## 目标和设计约束

优先修复可触发的内存边界和失败契约，随后加强门禁、减少重复执行。
维持现有组件所有权、单 executor、复制请求、latest-wins 完整帧和用户交互。
不借此重写 DOS DEBUG 命令、扩大文本/图形容量、改 Core、媒体格式或快照格式。
正常输入的输出须保持；错误输入明确失败，不能产生越界、假成功或静默漏记。

七项有限台账是本任务的收口范围。每项必须有实现证据、证明无需修改的理由，
或所有者批准的延期；不能用清单门禁通过代替动态语义证明。

## 问题台账与建议 S 拆分

### S1：xasm32 反汇编取码边界（P1）

- 源码：`src/x86/xasm32/{xasm32.c,dasm32.c}`；调用者
  `src/x86/debug/command.c`。
- 证据：公开入口只要求至少 15 字节；`_kdf_code()` 无界读取，`_kdf_skip()`
  仅递增，前缀循环没有上限。恰好 15 个 `0x66` 后仍会取第 16 字节。
  U/XU 使用 15 字节局部数组，因此该缺口能到达生产调用路径。
- 方案：在现有取码和位置推进入口、访问内存之前统一检查 15 字节上限，
  复用原有错误返回。不得只在公开调用返回后检查长度。扫描其他直接取码和
  汇编输出路径的同类边界，先证实可达性，再决定必要修复。
- 验证：保留 14 前缀加 NOP 的合法边界；补纯 15 前缀、尾部缺少立即数/
  ModRM/SIB/位移的失败，以及输出保持契约。用保护页或内存检查证明不越界；
  U/XU 与独立 xasm32 API 都需覆盖，保持合法命令原有输出。

### S2：Common 请求和队列通知失败（P2）

- 源码：`src/common/session/control.c`、`src/common/machine/machine.c`。
- 证据：Session 入队/故障槽忽略 event signal 结果；Machine 请求提交忽略
  reset/signal，媒体、debug、快照完成通知也忽略 signal。底层失败时可能
  接受请求却未唤醒消费者，或让同步调用永久等待；尚未注入故障复现。
- 方案：先列出所有提交、唤醒、完成和退出调用点，区分“尚未接受”和
  “已经接受且可能正在执行”。处理返回值并复用已有终止、取消及请求收尾。
  若完成通知失败，不得直接让调用者释放 executor 仍借用的 payload/context。
  不使用轮询重试、第二事件通道或每个出口一套清理。
- 验证：分别注入 reset、提交 signal、完成 signal 故障；证明拒绝不执行、
  接受不重放、调用者不会无限等待、借用数据在 worker 退出前仍有效。
  保留已修复的 debugger 关闭/暂停销毁竞争测试。
- 设计检查点：若现有等待/取消契约无法安全表达通知失败，先报告最小接口
  方案及影响，取得该边界变更批准；不得用增加平行状态来隐瞒这个限制。

### S3：Common 按键账本容量（P2）

- 源码：`src/common/session/{control.c,control.h}`。
- 证据：达到 256 项时记账静默返回，后续仍向机器发送该 make；来源退休
  无法为未记录的 make 补齐释放。
- 方案：新物理键超限时在下游投递前明确拒绝，进入既有输入失败处理；
  已存在键的重复 make 不应额外占槽。保留固定容量和唯一账本。
- 验证：填满、多来源、重复 make、拒绝后的来源退休；确认下游从未收到
  被拒绝的新 make，已接受的键仍按现有规则清理。

### S4：Common 帧序号回绕（P3）

- 源码：`src/common/machine/machine.c`、
  `src/common/session/{session.c,control_state.c}`，并检查 UI 的序号使用者。
- 证据：发布序号为递增 u32，Session 用普通大小关系过滤；观察值达到最大值
  后，回绕的序号被持续拒绝。这是极长运行边界，不是已复现的日常显示故障。
- 方案：统一产生、比较、零值和 run generation 之间的契约，保持旧帧过滤与
  新帧准入一致。准入时说明比较方案对最大未消费距离的假设，不能只改一处
  `>`，或让消费者通过清空所有状态来猜测回绕。
- 验证：临界序号、回绕、重复/过期通知、latest-wins 跳帧、重启以及新建
  presenter；用确定性状态测试，不等待真实计数器耗尽。

### S5：Common Types 边界门禁（P2）

- 源码：`src/common/verify_corpus.cmake`；测试
  `test/common/verify_negative.cmake`。
- 证据：生产 include 允许列表仍接受 `limits.h`、`stdio.h`、`stdlib.h`
  等直接标准头；已完成的 Types 边界迁移尚未形成完整防退化检查。
- 方案：按当前生产契约收紧直接标准头范围，补负例及合法 Types include
  正例。测试自身使用标准 C 库不因此被禁止。扫描 Lib/Common/x86 同类门禁，
  保留各自允许的边界，不建立第二套依赖权威。
- 验证：直接标准头和旧整数限制依赖被拒绝，规范引用通过；六份 manifest、
  依赖门禁及独立四/六目录构建继续通过。

### S6：xasm32 固定分派表精简

- 源码：`src/x86/xasm32/dasm32.c`。
- 证据：每次 `dasm32()` 清零新 context，再填入两个 256 项函数表，实例
  `initialized` 不会跨调用复用；这些 handler 映射本身不随解析状态改变。
- 方案：固定分派表改为文件内只读常量，删除 context 中的表及初始化标志。
  解析状态继续每次调用独立，不引入可变全局缓存；保持 handler 和原始输出。
- 验证：逐项核对全部 512 个映射；回归合法/非法 opcode、前缀和现有
  ModRM/SIB 遍历。报告实际栈占用、源码和只读数据变化，不预先承诺净减行数
  或运行加速。

### S7：Storage fill 合并刷新

- 源码：`src/lib/storage/medium.c`，必要的既有内部 file 操作。
- 证据：DIRECT 的 `fill_at()` 每 512 字节调用一次会 flush 的 `write_at()`；
  填充 1 MiB 对应 2048 次 flush。尚未测量实际性能收益。
- 方案：单次 fill 保留有界分块写入，在该操作结束前统一刷新；单次公开
  write 的既有语义保持。避免复制整套验证/写入实现，不增加公共 API。
  必须定义中途写失败时的刷新和错误优先级，不能仅把 flush 移到成功分支。
- 验证：空填充、跨页、尾页、DIRECT/READONLY/OVERLAY、写入和 flush 故障；
  通过调用计数证明减少刷新，成功返回时验证实际文件内容。错误路径仍明确
  报错，不声称提供事务回滚。无需长时间性能测量。

## 实施和退出标准

正式准入后按 S1--S7 串行执行。每个 S 开始前重新核对调用链，报告预计文件
和生产/测试新增、删除、净变化；完成后使用 Git diff 分别统计实际值，说明
保留路径及同类问题处置。S2 的通知失败方案须在修改前具体化。

每个代码 S 更新所属 manifest，执行针对性失败/边界测试、严格 C11 双宽度
构建及适用的后台回归，交付两份 package EXE；桌面测试采用已有隔离方案。
按项目 P 和实际变更复核规则提交推送，报告未覆盖平台，不将 Windows 通过
等同于 Linux 运行验收。用户 INI、磁盘、快照及主线程工作均不得被覆盖。

最终七项台账逐一闭环，六目录仍可按现有四/六组件组合独立复制构建；没有
新增平行 executor、输入账本、事件通道或可变解析全局状态。所有者验收后
才正式关闭未来准入的 T 任务。
