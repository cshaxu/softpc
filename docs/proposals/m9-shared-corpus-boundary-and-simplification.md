# Lib / Common / x86 边界修复与精简

## T80 准入

所有者请求：“测试通过 可以收口t任务 准入下一个t”。T79 已通过手测并收口，
本队首提案正式分配 T80；基线 `b9413d65`。以下七项沿用为 S1--S7。
S1--S4 已经所有者批准收口；T80 保持开放，S5 实现后等待手测，S6--S7 尚未准入。实际状态以 CURRENT 为准。
本次交接只修改文档，生产/测试代码 +0/-0，EXE 不变。S1 在动代码前先
复核取码、跳过和调用者路径，报告具体文件与增删估算，再执行实现与双宽度验证。

## 原始请求与准入边界

所有者先要求“审计一下，lib/common/x86六组件代码质量”，随后要求：
“将上述置于队列首位，写入proposal”。

本提案承接该次审计的全部七项结果，作为未编号队首候选。登记不分配 T 编号，
不准入实现，不改变 CURRENT 中的唯一活动任务。正式准入时重新冻结源码基线，
以下 S 编号仅表示建议的串行拆分。

审计读取的是 T78 实现 `0fb40f48` 所对应的共享源码；期间主线程继续更新治理
文档。六目录 `src/{lib,common,x86}`、`test/{lib,common,x86}` 共 204 个文件
完成清单核对，六份 manifest 和三个源码依赖检查通过。重点阅读了输入、帧、
同步请求、Storage 和 x86 编解码路径；这不等于逐行证明全部源码无缺陷。
此次未运行动态故障探针、虚拟机或完整回归。

## 目标和设计约束

优先修复可触发的内存边界和失败契约，随后加强门禁、减少重复执行。
维持现有组件所有权、单 executor、复制请求、latest-wins 完整帧和用户交互。
不借此重写 DOS DEBUG 命令、扩大文本/图形容量、改 Core、媒体格式或快照格式。
正常输入的输出须保持；错误输入明确失败，不能产生越界、假成功或静默漏记。

七项有限台账是本任务的收口范围。每项必须有实现证据、证明无需修改的理由，
或所有者批准的延期；不能用清单门禁通过代替动态语义证明。

## 问题台账与建议 S 拆分

### S1：xasm32 反汇编取码边界（P1）

实施前复核（`295c6963`）：唯一取码为 `_kdf_code`，推进为 `_kdf_skip`；
先检查推进再读取，保留现有错误传播。汇编三个原始写入口及最终前缀合并也补
写前容量保护，不改变编码器、公共接口或 U/XU。预计两个生产文件 +25/-3
（净 +22），测试/构建约 +130/-0；实际值交付时以 Git 统计为准。
有限扫描台账：反汇编取码/推进、六处 peek 回退、公开输入/输出契约、U/XU、
汇编三种写宽度/前缀合并/paragraph 最终复制。每项记录校验归属和回归证据。

保护页全 opcode 扫描发现原表唯一缺项 `F1`，调用空 handler。S1 将其接入
既有 `UndefinedOpcode`，沿用错误文案/返回路径，不新增指令实现；两张表其余 511 项均有
赋值。S6 的常量表重构仍未开始。首轮 U 测试误将一次 15 字节读取断言为
15 次请求，修正测试为一次，不修改 debugger 生产代码。

#### S1 实施台账

| 范围 | 实际处理与证明 |
| --- | --- |
| 取码/推进 | `_kdf_skip` 在推进前检查剩余 15 字节额度；`_kdf_code` 先完成该检查/推进再读取原位置，不新增检查层或状态。 |
| 六处 peek | INS_0F、INS_FF、INS_0F_00、INS_0F_01、INS_0F_BA、主循环均只回到本次读取前的位置；失败已在回退/分派前返回，无旁路取码。 |
| 公开入口/U/XU | 原入口仍要求至少 15 字节，输出仅成功时复制；U/XU 保持 15 字节缓存、语法和错误文案。补 15 前缀失败、14 前缀 NOP 成功/继续地址测试。 |
| 汇编写入 | 原三个宽度入口写前保护，48 个位置/宽度组合证明拒绝不改缓冲区或位置；前缀最多 11 字节，复制主体前核对合计长度，并置原 flagError，供 paragraph 原调用路径识别。 |
| paragraph | 已有两遍总容量检查后复制，不改变实现；原容量失败输出保持测试继续通过。未证明当前合法语法可生成超长指令，新增写保护是同类入口收口，不宣称存在已复现汇编溢出。 |
| opcode 分派 | 唯一 F1 空槽填为既有 UndefinedOpcode；其余 511 映射不变。保护页遍历两张表、双默认位宽、每个剩余字节数；S6 精简仍保留。 |

扫描命令：`rg -n 'drcode|iop' src/x86/xasm32/dasm32.c`、
`rg -n '_c_set(byte|word|dword)|rcode|acode' src/x86/xasm32/aasm32.c`、
`rg -n 'x86_xasm32_disassemble|ucode' src/x86/debug/command.c`，并枚举
两张表 0..255 的赋值。没有新增 public API、decoder、转发或全局可变状态。

源码冻结后的生产实际 +11/-2（净 +9）：aasm32.c +5/-0，dasm32.c +6/-2。
测试 C +148/-0，测试 CMake +1/-0（合计净 +149）；两份 manifest 不计代码。
相较预估生产净 +22 更小；测试多于预估 +130，原因是补全两张 opcode 表
扫描及三个汇编写入口的边界证明。Lib/Common/Core、INI、客体介质均不修改。

原版 `295c6963` 的保护页探针在 `_kdf_code` 读取第 16 字节触发访问异常；
修复后的相同探针通过。另以原版、新版并列解码 130,050 组双默认位宽的
两字节组合（排除原来 F1 空槽），长度和完整输出相同；这不是全指令语义证明。
独立 test/x86 构建双宽度使用 C11、`-Wall -Wextra -Wpedantic -Werror`，
两边各 10/10 通过（x64 4.75s、x86 6.56s）。没有 Linux 运行验收声明。

最终两套 Release 构建通过；后台 `ctest --preset test-x64/test-x86` 分别
111/111（136.13s）与 111/111（140.98s）通过，每边五个 desktop 项未运行。
六份 manifest、组件 DAG、文档门禁和 diff-check 通过。首次中间测试中的
U 请求计数和带前缀 UndefinedOpcode 返回断言已按原语义纠正；修改尚未冻结
时的 manifest 不匹配不作为验收。一次启动往返超时停于图形帧，未修改其代码
或超时；最终双宽度通过。该二进制不含 xasm32 符号，未发现本修改因果关系，
也不据单次重跑宣称已修复其时序脆弱性。

| S1 产物 | 字节 | SHA256 |
| --- | ---: | --- |
| softpc32.exe | 3695176 | CC7476D0EF7FD32D461A72552AA305468DF0DE80DE2575595453CE4660AB3AE3 |
| softpc64.exe | 3080555 | 72EFB4BC03EA8F27FC1945400246D8A3AD6E28CF78271E40A77FD74AB915E639 |

产物位于标准 `assets/binary/`，INI 原样保留。S1 executor `73a5a889`、实际
变更复核 `f4941481` 后，所有者要求“收口s1 准入s2”；S1 正式收口，T80 开放。

- 源码：`src/x86/xasm32/{xasm32.c,dasm32.c}`；调用者
  `src/x86/debug/command.c`。
- 证据：公开入口只要求至少 15 字节；`_kdf_code()` 无界读取，`_kdf_skip()`
  仅递增，前缀循环没有上限。恰好 15 个 `0x66` 后仍会取第 16 字节。
  U/XU 使用 15 字节局部数组，因此该缺口能到达生产调用路径。
- 方案：在现有取码和位置推进入口、访问内存之前统一检查 15 字节上限，
  复用原有错误返回。不得只在公开调用返回后检查长度。扫描其他直接取码和
  汇编输出路径的同类边界，先证实可达性，再决定必要修复。
- 验证：保留 14 前缀加 NOP 的合法边界；补纯 15 前缀、尾部缺少立即数/
  ModRM/SIB/位移的失败，以及输出保持契约。用保护页或内存检查证明不越界；
  U/XU 与独立 xasm32 API 都需覆盖，保持合法命令原有输出。

### S2：Common 请求和队列通知失败（P2）

#### 所有者批准的范围收窄与收口

以下原始修复计划保留作追溯，不再作为待执行承诺。所有者质疑新增恢复机制
是否过度复杂，批准先审计正常调用契约下的实际可达性；在听取结果和异常
限制后回复“批准收口”。S2 因此以审计完成、不修改生产实现收口，不宣称
所有通知失败已修复。取消新增致命故障回调、备用通知和请求状态扩建方案。

基线 `1b5d0fd7`；[S2 历史记录](../history/M9-T80-S2-notification-audit.md)
保存有限路径台账、证据和保留限制。生产/测试 +0/-0；既有 Common 测试
x64/x86 各 18/18，三项重点测试各宽度各重复二十次通过。没有重新编译、
新增故障注入、全产品或 Linux/桌面验收。此前要求的新故障探针及双 EXE
重建随本次不改代码的收窄取消，原有 EXE 保持不变。

未发现正常契约下新的丢唤醒、重复执行或提前销毁路径，但仍有已披露的
异常限制：App 快照调用无条件关闭 reader/writer，而原生等待失败不保证
executor 不再借用 context。未找到有效同步对象下该等待失败的正常触发路径。
所有者接受本次不处理、不扩建恢复体系；这是接受限制，不是修复证明或隐含
后续任务。若出现真实原生等待失败/生命周期证据，再由所有者决定重新准入。

#### 原始计划（由上述收窄决定替代）

准入基线 `f4941481`。先冻结提交、唤醒、完成、退出四类路径及其数据所有权，
再给出具体方案、文件范围和代码增删预估；本次准入仅改治理文档，不把尚未
审计的失败处理方案当成可执行补丁。若需变更公开等待/取消契约，仍先报批。

- 源码：`src/common/session/control.c`、`src/common/machine/machine.c`。
- 证据：Session 入队/故障槽忽略 event signal 结果；Machine 请求提交忽略
  reset/signal，媒体、debug、快照完成通知也忽略 signal。底层失败时可能
  接受请求却未唤醒消费者，或让同步调用永久等待；尚未注入故障复现。
- 方案：先列出所有提交、唤醒、完成和退出调用点，区分“尚未接受”和
  “已经接受且可能正在执行”。处理返回值并复用已有终止、取消及请求收尾。
  若完成通知失败，不得直接让调用者释放 executor 仍借用的 payload/context。
  不使用轮询重试、第二事件通道或每个出口一套清理。
- 验证：分别注入 reset、提交 signal、完成 signal 故障；证明拒绝不执行、
  接受不重放、调用者不会无限等待、借用数据在 worker 退出前仍有效。
  保留已修复的 debugger 关闭/暂停销毁竞争测试。
- 设计检查点：若现有等待/取消契约无法安全表达通知失败，先报告最小接口
  方案及影响，取得该边界变更批准；不得用增加平行状态来隐瞒这个限制。

### S3：Common 按键账本容量（P2）

所有者在 S2 审计收口后要求“下一个s任务准入”。准入只更新文档，生产基线
仍为 `1b5d0fd7`。复核确认 remember 在满容量时静默返回，但 dispatch 继续
调用 sink。收窄方案：现有 remember 返回是否记账成功，dispatch 失败即返回，
复用既有输入失败处理；保留 forget 去重、256 容量及物理键身份定义。
预计 `control.c` +5/-3（净 +2），`physical_key_identity_smoke.c` 新增约
60--90 行；另更新两个 manifest 和任务文档。边界、满容量重复 make、
跨来源新键拒绝、释放后复用及退休清理均须验证。交付后等待所有者测试。

- 源码：`src/common/session/{control.c,control.h}`。
- 证据：达到 256 项时记账静默返回，后续仍向机器发送该 make；来源退休
  无法为未记录的 make 补齐释放。
- 方案：新物理键超限时在下游投递前明确拒绝，进入既有输入失败处理；
  已存在键的重复 make 不应额外占槽。保留固定容量和唯一账本。
- 验证：填满、多来源、重复 make、拒绝后的来源退休；确认下游从未收到
  被拒绝的新 make，已接受的键仍按现有规则清理。

#### S3 实施与同类扫描

所有者批准修复后，先增加容量测试：原生产代码在“满容量的新键必须被拒绝”
断言处失败。随后只改原 remember 返回值和 dispatch 的投递前检查，保持原
forget 去重、固定数组和单控制线程。真实调用者收到失败后沿原 Session run
失败出口返回，再由 App 执行现有 shutdown；不新增错误事件或继续输入策略。

| 有限检查范围 | 处理/证明 |
| --- | --- |
| 唯一记账入口 | `remember_pressed` 返回 0/1；新键超限不写数组、不调用 sink。 |
| 已有键重复 make | 原 forget 先释放该键的槽位，再写回，满容量时仍成功，容量不变。 |
| 物理身份/来源 | 同扫描码跨来源独立；扩展位仍参与身份；现有零扫描码/左右键测试保留。 |
| release/退休 | 原实现不变；释放一个槽位后可接收新键，拒绝来源无 release，退休不重复补发。 |
| 失败传播 | `dispatch_input` -> `handle_kvm_input` -> `session_run` 原失败返回 -> App 原清理；没有第二路径。 |

搜索：`rg -n 'pressed_count|remember_pressed|dispatch_input' src/common/session`。
生产只有 control.c 改动：+6/-4，净 +2，与预估净值一致；显式分支花括号
使两侧各比预估多一行。测试 physical_key_identity_smoke.c +56/-0，比预估
60--90 少，通过同一原 fixture 验证填满、重复、同来源扩展新键、跨来源新键、
拒绝后退休、释放/重用和重复退休。总生产+测试 +62/-4，净 +58；两个
manifest、EXE 和文档另计。无公开 ABI、Lib、Core、x86、INI 或介质修改。
最终两套 Release 构建通过，Common 编译采用 C11、严格四项 warning flags。
后台全量 x64 111/111（141.46s），x86 111/111（140.30s）；每边五个 desktop
项未运行，无新独立构建或 Linux 运行声明。所有源码/测试 manifest、DAG 和
最终文档门禁通过。executor `aea41c16`、复核 `2cd9e889` 交付后，所有者
要求“收口s3准入s4”，S3 正式收口；T80 保持开放。

| S3 产物 | 字节 | SHA256 |
| --- | ---: | --- |
| softpc32.exe | 3695688 | F8C42F5654B05D2B8720E3704A56197EC35D52139F736F9312317ED0CEAD0B89 |
| softpc64.exe | 3080555 | E84CBE10EA5E0FA12ABDDFCAA94FEB7FBA19F293455B32DF628C99AF54B0582D |

对应标准 assets/binary 产物；INI 不变。与 S1 包相比 x86 文件 +512 字节，
x64 文件大小不变；这只是最终文件大小，不等同于运行内存或机器码增加量。
[S3 记录](../history/M9-T80-S3-pressed-key-capacity.md)保留实际变更复核。

### S4：Common 帧序号回绕（P3）

所有者在接受 S3 后准入本项；基线 `2cd9e889`，本次交接仅改文档。
初步复核：Machine 直接递增 u32，Session 通知入口与 reducer 分别使用
`>` 和 `<=`；UI 用相等性去重并以 0 初始化。拟保留字段宽度，统一模序号
比较并保留 0 的未发布约定。实现前必须核对跨 run、旧通知、首次消费和
latest-wins 的最大距离约束；若采用半范围比较，必须明确未消费距离小于
2^31 的前提，不能把它宣称为任意长暂停下无条件正确。
预计约四个生产文件 +20--30/-5--10（净 +10--25），测试 +80--140/-0；
实际文件与算法在完整序号路径审计后冻结。公共字段宽度不变，不改 Lib/Core。

- 源码：`src/common/machine/machine.c`、
  `src/common/session/{session.c,control_state.c}`，并检查 UI 的序号使用者。
- 证据：发布序号为递增 u32，Session 用普通大小关系过滤；观察值达到最大值
  后，回绕的序号被持续拒绝。这是极长运行边界，不是已复现的日常显示故障。
- 方案：统一产生、比较、零值和 run generation 之间的契约，保持旧帧过滤与
  新帧准入一致。准入时说明比较方案对最大未消费距离的假设，不能只改一处
  `>`，或让消费者通过清空所有状态来猜测回绕。
- 验证：临界序号、回绕、重复/过期通知、latest-wins 跳帧、重启以及新建
  presenter；用确定性状态测试，不等待真实计数器耗尽。

#### S4 实施与有限序号台账

所有者确认自动回绕方案后要求“批准修复”。实际执行基线 `3284fd89`。
收窄后的预估为生产四文件 +15--22/-4--7、净 +11--15，测试
+80--120/-0--5。实现保留 u32，发布跳过 0；Session 的两处排序共享
control_state.h 内一个私有 inline 函数。不新增计数器、缓存或公共接口。
比较采用无符号模距离，要求比较位置间距离小于 2^31；恰好半圈拒绝。
这不承诺消费者落后半圈以上时还能无歧义恢复顺序。

冻结范围为 Common Machine 发布、Session 接收/reducer、UI 去重/重建；
单位是序号生成或消费路径。完成谓词是每项有修复或保留理由及测试，
不是全树任意 generation 计数器的回绕证明。

| 序号路径 | 处理与证明 |
| --- | --- |
| Machine 唯一发布入口 | 最大序号之后发布 1，不发布 0；未变化文本不额外递增。machine_wait 覆盖。 |
| cold run | 保留原有对象级计数器，清除帧缓存但不重置序号；跨重启回绕测试通过此路径。 |
| Session 通知与复制后确认 | 两处共用模比较；旧通知可取到回绕后的最新完整帧；重复、0 和旧序号不触发再复制。session_frame 覆盖。 |
| 初次消费 | previous=0 接受任意非零首帧，包括 UINT32_MAX；不把它错误认作落后。 |
| run generation | 保留事件前置检查和带 generation 的帧复制检查；旧 run 拒绝，新 run 正常接收，现有复制期间换 run 测试保留。 |
| UI 去重与重建 | 仅比较相等，不排序；生产代码无需修改。composition 覆盖 MAX→1、重复帧及重建后同序号重新提交。 |

搜索：`rg -n 'sequence' src/common/machine/machine.c src/common/session src/common/ui/ui.c`。
generation 的身份匹配不改成帧排序；未发现另一处帧序号大小比较遗漏。

实际生产：machine.c +4/-1、control_state.h +9/-0、control_state.c +2/-2、
session.c +1/-1，合计 **+16/-4，净 +12**。测试 machine_wait +12、
session_frame +40、composition +17，合计 **+69/-0**；复用 fixture 使其
小于预估。生产加测试 **+85/-4，净 +81**。manifest、文档、EXE 另计；
Lib/Core/x86、公共字段、用户 INI 与介质均未改变。

先在旧生产代码上运行新增 machine_wait/session_frame：前者回绕发布 0，
后者拒绝 MAX→1，两项均失败，确认回归测试能识别原缺陷。
两套 Release 构建通过，Common 使用 C11、Wall/Wextra/Wpedantic/Werror。
针对性三项两边均 3/3；后台全量 x64 111/111（173.24s），x86 111/111
（160.81s）。每边五个 desktop 测试明确未运行，无新 Linux 运行或独立
包构建声明。六份 manifest、依赖门禁与最终文档治理通过。

| S4 产物 | 字节 | SHA256 |
| --- | ---: | --- |
| softpc32.exe | 3695688 | 381771DA47C16274E70272F9369F6920E83E9FEB9DB43ED4CD76B8A051CEEA07 |
| softpc64.exe | 3080555 | 61CAA22FDCAB1B691FEDCC41F6B9698BB588087E1E4CEA9E9C2301EA1514A6D9 |

标准 package EXE 大小相对 S3 均不变，不代表运行内存或机器码大小完全不变。
[S4 记录](../history/M9-T80-S4-frame-sequence-wrap.md)承接 executor 后的
实际变更复核。交付后所有者批准收口 S4 并准入 S5；T80 保持开放。

### S5：Common Types 边界门禁（P2）

所有者请求“批准收口S4，准入S5开始修复”，执行基线 `0be6a12c`。
预审发现 machine.c 七处 UINT32_MAX 依赖 Types 传递包含，改用现有
LIB_UINT32_MAX 即可，数值/行为不变。不引入新 parser、公共 API 或门禁框架。
预计运行时 +7/-7，门禁 +4--8/-4--8，测试 +25--40/-0，manifest/文档另计。
冻结检查范围为 Common C/H 的字面 include 与整数上限宏、Lib/x86 对应门禁；
各命中记录修复或保留理由，不宣称实现完整 C 预处理器验证。

- 源码：`src/common/verify_corpus.cmake`；测试
  `test/common/verify_negative.cmake`。
- 证据：生产 include 允许列表仍接受 `limits.h`、`stdio.h`、`stdlib.h`
  等直接标准头；已完成的 Types 边界迁移尚未形成完整防退化检查。
- 方案：按当前生产契约收紧直接标准头范围，补负例及合法 Types include
  正例。测试自身使用标准 C 库不因此被禁止。扫描 Lib/Common/x86 同类门禁，
  保留各自允许的边界，不建立第二套依赖权威。
- 验证：直接标准头和旧整数限制依赖被拒绝，规范引用通过；六份 manifest、
  依赖门禁及独立四/六目录构建继续通过。

#### S5 有限命中台账与实际变更

| 检查单元 | 命中与处置 |
| --- | --- |
| Common 字面 include | 生产 C/H 已全部使用 common/lib 规范路径；删除门禁中十二种标准头的旧白名单。相同规则覆盖 C、私有 H 和公共 H，不再另留 stdint 公共头特判。 |
| Common 原始整数上限宏 | machine.c 七处 UINT32_MAX：暂停等待两处、外层等待一处、media/state/debug 等待四处。全部等值替换为现成 LIB_UINT32_MAX；等待时间与失败路径不变。 |
| 新门禁验证 | 十二种头各验证尖括号/引号写法，共24负例；八种上限宏各验证表达式/宏别名，共16负例；公共头1负例；Types、合法链接及注释/字符串1正例。复用既有 probe，不新增框架。 |
| Lib 门禁 | verify_types_layout 已从 Types alias 和独立整数家族检查原始词汇，非 Types 外部 include 被拒绝；已有负例。保持不变。 |
| x86 门禁 | verify_corpus 已拒绝标准头；生产无所扫描的裸整数上限宏，现有 LIB 宏与导入实现的 STD_SIZE_MAX 不混同。保持本次 Common 范围，不扩写 x86 检查器。 |
| 测试自身 | 共享测试仍可使用标准 C 库；新限制只检查 Common 生产 corpus。 |

搜索为 Common C/H include 枚举，以及三套源码的完整 token
`UINT_MAX|INT32_MAX|INT32_MIN|UINT32_MAX|UINT64_MAX|SIZE_MAX`；复核 Lib 的
types-layout 与 x86 corpus gate。此为有界字面源检查，不宣称宏展开/任意
预处理写法均有编译器级证明。无本次生产命中延期。

实际三文件：machine.c **+7/-7**；verify_corpus.cmake **+4/-4**；
verify_negative.cmake **+24/-0**。运行时和门禁各净零，合计 **+35/-11，
净 +24**，全部净增来自测试。比测试预估少一行，复用原 fixture 即可。
两份 manifest、治理文档和 EXE 另计。Lib/Core/x86 和公开 ABI 均不变。
新增负例先在旧门禁上失败（assert.h 被放行），修复后 Common corpus 与
verifier-negative 两项通过。最终双宽度 Release 构建均通过；产品后台
x64 111/111（273.67s），x86 111/111（178.46s），各排除五个 desktop 项。

独立复制四目录（无 x86）与六目录，四个严格 C11 构建均通过；复制目录不含
App/Core、产品构建目标或介质。两套套件通过各自原有 CMake 入口组装：

| 复制组合 | x64 后台 | x86 后台 |
| --- | --- | --- |
| 四目录 Lib/Common | 59/59，95.24s | 59/59，98.94s |
| 六目录 Lib/Common/x86 | 69/69，90.14s | 69/69，96.77s |

每套隔离测试排除三项 desktop。六份 manifest 与组件门禁通过；无 Linux
运行验收或下游项目集成声明。四/六目录分别180/205文件在构建前后逐字节
相同；证据记录后删除本次 build/t80-s5 隔离副本/构建目录，可从仓库重建。

| S5 产物 | 字节 | SHA256 |
| --- | ---: | --- |
| softpc32.exe | 3695688 | 2F51A972247D074F9B13277E1B4306AA3218C8C1452D767A219AA29D85623ED1 |
| softpc64.exe | 3080555 | AA70762F182682B42F26B94D53CD7A0BD062BE91F85315C47EC6AA78ADB7844E |

两份 EXE 大小相对 S4 不变，INI/media 不变。交付及实际变更复核见
[S5记录](../history/M9-T80-S5-common-types-gate.md)。待所有者手测后才收口 S5。

### S6：xasm32 固定分派表精简

- 源码：`src/x86/xasm32/dasm32.c`。
- 证据：每次 `dasm32()` 清零新 context，再填入两个 256 项函数表，实例
  `initialized` 不会跨调用复用；这些 handler 映射本身不随解析状态改变。
- 方案：固定分派表改为文件内只读常量，删除 context 中的表及初始化标志。
  解析状态继续每次调用独立，不引入可变全局缓存；保持 handler 和原始输出。
- 验证：逐项核对全部 512 个映射；回归合法/非法 opcode、前缀和现有
  ModRM/SIB 遍历。报告实际栈占用、源码和只读数据变化，不预先承诺净减行数
  或运行加速。

### S7：Storage fill 合并刷新

- 源码：`src/lib/storage/medium.c`，必要的既有内部 file 操作。
- 证据：DIRECT 的 `fill_at()` 每 512 字节调用一次会 flush 的 `write_at()`；
  填充 1 MiB 对应 2048 次 flush。尚未测量实际性能收益。
- 方案：单次 fill 保留有界分块写入，在该操作结束前统一刷新；单次公开
  write 的既有语义保持。避免复制整套验证/写入实现，不增加公共 API。
  必须定义中途写失败时的刷新和错误优先级，不能仅把 flush 移到成功分支。
- 验证：空填充、跨页、尾页、DIRECT/READONLY/OVERLAY、写入和 flush 故障；
  通过调用计数证明减少刷新，成功返回时验证实际文件内容。错误路径仍明确
  报错，不声称提供事务回滚。无需长时间性能测量。

## 实施和退出标准

正式准入后按 S1--S7 串行执行。每个 S 开始前重新核对调用链，报告预计文件
和生产/测试新增、删除、净变化；完成后使用 Git diff 分别统计实际值，说明
保留路径及同类问题处置。S2 的通知失败方案须在修改前具体化。

每个代码 S 更新所属 manifest，执行针对性失败/边界测试、严格 C11 双宽度
构建及适用的后台回归，交付两份 package EXE；桌面测试采用已有隔离方案。
按项目 P 和实际变更复核规则提交推送，报告未覆盖平台，不将 Windows 通过
等同于 Linux 运行验收。用户 INI、磁盘、快照及主线程工作均不得被覆盖。

最终七项台账逐一闭环，六目录仍可按现有四/六组件组合独立复制构建；没有
新增平行 executor、输入账本、事件通道或可变解析全局状态。所有者验收后
才正式关闭未来准入的 T 任务。
